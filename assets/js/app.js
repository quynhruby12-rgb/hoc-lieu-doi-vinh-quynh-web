
async function getJSON(path){const r=await fetch(path,{cache:'no-store'});if(!r.ok)throw new Error(`Không tải được ${path}`);return r.json()}const safe=(v,f='')=>v??f;function attrs(link,download=false){return `href="${link||'#'}" ${download?'download':''}`}
async function init(){try{const [site,topics,resources,activities,quiz]=await Promise.all(['site','topics','resources','activities','quiz'].map(x=>getJSON(`data/${x}.json`)));document.title=`${site.site_title} - ${site.school_name}`;document.querySelectorAll('[data-school]').forEach(e=>e.textContent=site.school_name);for(const [k,v] of Object.entries({'slogan':site.slogan,'site-title':site.site_title,'site-subtitle':site.site_subtitle,'hero-badge':site.hero_badge,'hero-title':site.hero_title,'hero-intro':site.hero_intro,'footer-description':site.footer_description,'email':site.contact_email,'manager':site.manager_name}))document.querySelectorAll(`[data-${k}]`).forEach(e=>e.textContent=v);document.querySelector('[data-hero-image]').src=site.hero_image;for(const [k,v] of Object.entries({'topics-label':topics.section_label,'topics-title':topics.section_title,'topics-intro':topics.section_intro,'resources-label':resources.section_label,'resources-title':resources.section_title,'resources-intro':resources.section_intro,'activities-label':activities.section_label,'activities-title':activities.section_title,'activities-intro':activities.section_intro,'quiz-title':quiz.title,'quiz-question':quiz.question}))document.querySelector(`[data-${k}]`).textContent=v;document.querySelector('[data-topics-list]').innerHTML=topics.items.map(x=>`<article class="card"><div class="icon">${safe(x.icon,'📘')}</div><h3>${x.title}</h3><p>${x.description}</p><a class="more" ${attrs(x.link)}>Xem nội dung →</a></article>`).join('');document.querySelector('[data-resources-list]').innerHTML=resources.items.map(x=>`<article class="learn"><div class="thumb">${x.image?`<img src="${x.image}" alt="">`:safe(x.icon,'📚')}</div><div class="body"><span class="tag">${x.type}</span><h3>${x.title}</h3><p>${x.description}</p><p><a class="more" ${attrs(x.link,x.download)}>Mở học liệu →</a></p></div></article>`).join('');document.querySelector('[data-activities-list]').innerHTML=activities.items.map(x=>`<article class="g" style="${x.image?`background-image:linear-gradient(0deg,rgba(0,0,0,.6),transparent),url('${x.image}')`:''}"><div>${x.title}<small>${x.description}</small></div></article>`).join('');const opts=document.querySelector('[data-quiz-options]');opts.innerHTML=quiz.answers.map((a,i)=>`<button class="opt" data-index="${i}">${String.fromCharCode(65+i)}. ${a}</button>`).join('');opts.querySelectorAll('.opt').forEach(btn=>btn.onclick=()=>{opts.querySelectorAll('.opt').forEach(b=>b.disabled=true);const i=Number(btn.dataset.index);if(i===quiz.correct_index){btn.classList.add('correct');document.querySelector('[data-quiz-feedback]').textContent=quiz.success_message}else{btn.classList.add('wrong');opts.querySelector(`[data-index="${quiz.correct_index}"]`).classList.add('correct');document.querySelector('[data-quiz-feedback]').textContent=quiz.failure_message}});document.querySelector('[data-full-quiz]').href=quiz.full_quiz_link}catch(e){document.querySelector('main').insertAdjacentHTML('afterbegin',`<div class="container error">Không tải được dữ liệu. Hãy chạy qua hosting hoặc web server. ${e.message}</div>`)}}document.addEventListener('DOMContentLoaded',init);
